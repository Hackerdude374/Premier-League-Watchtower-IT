docker compose down
docker compose up --build


in ROOT FOLDER
docker compose down -v
to reset database ^ dont do this if not needed 

Powershell post commands 


PowerShell’s curl is an alias for Invoke-WebRequest, so -X errors out.

POST refresh (PowerShell)


Invoke-WebRequest -Uri "http://localhost:8000/standings/refresh" -Method POST | Select-Object -ExpandProperty Content


GET health


Invoke-WebRequest -Uri "http://localhost:8000/health" | Select-Object -ExpandProperty Content


Or use real curl:

curl.exe -X POST http://localhost:8000/standings/refresh
curl.exe http://localhost:8000/health


Invoke-WebRequest -Uri "http://localhost:8000/standings/refresh" -Method POST | Select-Object -ExpandProperty Content
Invoke-WebRequest -Uri "http://localhost:8000/standings" | Select-Object -ExpandProperty Content

Run IT bnackend py tests for testing 

docker compose exec backend pytest -q

docker compose down
docker compose up --build -d
docker compose exec backend pytest -q


http://localhost:8080/

